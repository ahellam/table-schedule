// import { setupWorker, rest } from 'msw'

// const worker = setupWorker(
//   rest.get("http://localhost:3000/employees?sort_by=first_name", (req, res, ctx) => {
//     return res(ctx.json({

//     }))
//   })
// )

// worker.start()